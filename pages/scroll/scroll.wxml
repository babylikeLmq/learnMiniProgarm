
<!-- 水平滚动 -->
<scroll-view class="container1" scroll-x>
  <view wx:for="{{10}}" class="item1">{{item}}</view>
</scroll-view>
<!-- 垂直滚动 -->
<scroll-view class="container2" scroll-y>
  <view wx:for="{{10}}" class="item2">{{item}}</view>
</scroll-view>
滚动到顶部/左边时触发 bindscrolltoupper
滚动到底部/右边时触发 bindscrolltolower
<scroll-view class="container2" scroll-y bindscroll="handleScroll"
  bindscrolltoupper="top" upper-threshold="100"
  bindscrolltolower="bottom" lower-threshold="0"

>
  <view wx:for="{{10}}" class="item2">{{item}}</view>
</scroll-view>